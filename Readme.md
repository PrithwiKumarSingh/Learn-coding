 go to github make a repository then copy the link of github repo.
 before push set origin on the server using <git remote add origin https://github.com/PrithwiKumarSingh/github-crash-course.git>

 then <git remote -v> for comformation to set the origin main then all set.

 after these process <git push -u origin main> for push the all code on our github repository.